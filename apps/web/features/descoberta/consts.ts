/** Quantas categorias listar em /categorias (cards do ranking diário). */
export const CATEGORIES_LIMIT = 12

/** Quantos produtos exibir na grid do /produtos (o adapter pagina ≤10/pág). */
export const PRODUCTS_LIMIT = 24
